# Documentation for IDA Launcher

It is accessible via [http://docs.launcher.ida.dcs.gla.ac.uk](http://docs.launcher.ida.dcs.gla.ac.uk).

You can build documentation via:

```bash
pip install -r requirements.txt
make html
```
